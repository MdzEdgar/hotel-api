doctype html
html
    head
        meta(charset='utf-8')
        meta(name='viewport' content='width=device-width, initial-scale=1.0')
        title Password Reset Request
        style.
            body {
                margin: 0;
                padding: 0;
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                background-color: #f4f4f4;
            }
            .container {
                width: 100%;
                border-collapse: collapse;
            }
            .email-wrapper {
                width: 600px;
                margin: 40px auto;
                background-color: #ffffff;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }
            .header {
                padding: 40px 30px;
                background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
                text-align: center;
            }
            .header h1 {
                margin: 0;
                color: #ffffff;
                font-size: 28px;
                font-weight: 600;
            }
            .content {
                padding: 40px 30px;
            }
            .content p {
                margin: 0 0 20px;
                font-size: 16px;
                line-height: 1.6;
                color: #333333;
            }
            .button-wrapper {
                text-align: center;
                margin: 30px 0;
            }
            .button {
                display: inline-block;
                padding: 16px 36px;
                font-size: 16px;
                color: #ffffff;
                text-decoration: none;
                border-radius: 6px;
                font-weight: 600;
                background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            }
            .link-text {
                margin: 0 0 20px;
                font-size: 14px;
                line-height: 1.6;
                color: #666666;
            }
            .link-text a {
                color: #f5576c;
                word-break: break-all;
            }
            .warning-box {
                margin: 30px 0;
                padding: 20px;
                background-color: #fff3cd;
                border-left: 4px solid #ffc107;
                border-radius: 4px;
            }
            .warning-box p {
                margin: 0;
                font-size: 14px;
                line-height: 1.6;
                color: #856404;
            }
            .footer {
                padding: 30px;
                background-color: #f8f9fa;
                text-align: center;
                border-top: 1px solid #e9ecef;
            }
            .footer p {
                margin: 0 0 10px;
                font-size: 14px;
                color: #666666;
            }
            .footer .copyright {
                margin: 0;
                font-size: 12px;
                color: #999999;
            }
            @media only screen and (max-width: 600px) {
                .email-wrapper {
                    width: 100% !important;
                }
            }
    body
        table.container(role='presentation')
            tr
                td(align='center' style='padding: 40px 0;')
                    table.email-wrapper(role='presentation')
                        // Header
                        tr
                            td.header
                                h1 Password Reset Request

                        // Content
                        tr
                            td.content
                                p Hi #[strong #{userName}],
                                p We received a request to reset your password. Click the button below to create a new password:

                                .button-wrapper
                                    a.button(href=resetLink target='_blank') Reset Password

                                p.link-text
                                    | Or copy and paste this link into your browser:
                                    br
                                    a(href=resetLink)= resetLink

                                .warning-box
                                    p
                                        strong ⚠️ Important:
                                        |  This link will expire in #[strong #{expiryMinutes} minutes].

                                p(style='margin: 0; font-size: 14px; line-height: 1.6; color: #666666;')
                                    | If you didn't request a password reset, please ignore this email or contact support if you have concerns.

                        // Footer
                        tr
                            td.footer
                                p For security reasons, never share this link with anyone.
                                p.copyright © #{currentYear} Your Company. All rights reserved.